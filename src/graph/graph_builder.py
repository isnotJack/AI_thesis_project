"""
Modulo di costruzione del grafo statico (Blocco B).

Da implementare dopo che il Blocco A avra' prodotto i JSON in
data/processed/extracted_json/<ISO3>/. Compiti previsti:
- creare un nodo per paese, con attributo "profilo" = JSON estratto
  (piu' recente o aggregato, da decidere)
- creare archi di tipo migrazione / diplomatico / cyber a partire
  dai campi corrispondenti nei JSON estratti
- serializzare il grafo finale in data/processed/graphs/ (networkx, pickle o graphml)

Placeholder: non ancora implementato.
"""

# TODO: def build_nodes(paesi: list[dict], extracted_dir: Path) -> None: ...
# TODO: def build_edges(G, extracted_data: dict) -> None: ...
# TODO: def save_graph(G, path: Path) -> None: ...
